# dogage

這是一個簡單的靜態網站專案（範例）。

檔案結構：
- `index.html`
- `css/`
- `js/`
- `images/`

如何使用：
1. 在本機打開 `index.html`。
2. 使用 Git 初始化並提交：
   - `git init`
   - `git add README.md`
   - `git commit -m "first commit"`

作者：你的名字（可選）
